// a) Arrow function to check if number is even
const isEven = (n) => n % 2 === 0;
// b) Ternary operator
const result = marks >= 35 ? "Pass" : "Fail";
// c) Arrow function with ternary operator and default parameter
const greet = (name) => console.log(name ? `Hello, ${name}` : "Guest");
